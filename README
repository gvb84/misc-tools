resolver.c - a poor man's asynchronous DNS resolver written in C

usscan.c - a UNIX socket scanner which might come in handy when checking local
UNIX socket access permissions to search for local privilege escalation
vulnerabilities. Linux only for now.

refhack.html - HTTP referrer hack which is a stripped down version of
https://code.google.com/p/referrer-killer/

sslsniff.patch - Quick sslsniff patch which logs in hex format and doesn't
assume that it's HTTP+SSL that runs over port 443.

dietlibc-0.33-python3.3.patch - Quick patch to compile Python 3.3 with
dietlibc-0.33. The patch fixes missing wcstrtok and two defines needed for
modern Linux systems. 

pnghash.c - calculates perceptual hash values over PNG files which can be used
to find similarly looking PNG files relatively quickly.
